#!/bin/sh

# open a new gui frame of emacs to edit files

set -eu

. "$(cd -- "$(dirname -- "$0")" >/dev/null 2>&1 && pwd)/_emacsclient.sh"

emacsclient_ensure_running
emacsclient -q -n -c
emacsclient_gui_open "#@"
